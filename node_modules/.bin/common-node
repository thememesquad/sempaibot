#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../common-node/bin/common-node" "$@"
  ret=$?
else 
  node  "$basedir/../common-node/bin/common-node" "$@"
  ret=$?
fi
exit $ret
